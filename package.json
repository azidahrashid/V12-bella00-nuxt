{
  "name": "bella00-nuxt-scaffold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "extract": "node scripts/extract-site.mjs",
    "clean-css": "node scripts/clean-css.mjs",
    "convert": "node scripts/convert-to-nuxt.mjs",
    "migrate": "npm run extract -- https://www.bella00.com/ && npm run clean-css && npm run convert"
  },
  "dependencies": {
    "nuxt": "^3.15.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
